import {css} from "styled-components";

export const v = {
    sidebarWidth: "30rem",
    smSpacing: ".8rem",
    mdSpacing: "1.6rem",
    lgSpacing: "2.4rem",
    xlgSpacing: "3.2rem",
    xxlgSpacing: "4.8rem",
    borderRadius: ".5rem",
}

export const btnReset = css`
    font-family: inherit;
    outline: none;
    border: none;
    background: none;
    letter-spacing: inherit;
    color: inherit;
    font-size: inherit;
    text-align: inherit;
    padding: 0;
`